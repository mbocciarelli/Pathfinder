#pragma once

#include <string>

namespace InputManager {
/*
    enum ContentTypeName {
        TEXTFIELD,
        BUTTON,
        TEXT,
        IMAGE,
        NONE
    };

    class ContentType {
    public:
        ContentTypeName _type;

        ContentType() {
            _type = NONE;
        };

        ContentType(ContentTypeName type) {
            _type = type;
        }

        ~ContentType() {}

        ContentTypeName GetType() {
            return _type;
        }

        void SetType(ContentTypeName type) {
            _type = type;
        }

    };
    */
}
